$(function(){
    // $('select').styler();
    // $('.stages__item').hover(
    //     function(){ 
    //         $(this).find('.stages__item-img').toggleClass('visible');
    //     }

    // );

    $('select').selectric();
});